# 基于条纹特征的东北虎个体识别模型
构建了一种基于条纹特征的东北虎个体识别模型，主要用到SIFT（尺度不变特征变换）、Fisher向量（FV）和多层感知机（MLP）算法。首先利用SIFT算法提取东北虎侧腹部条纹稳定区域的局部关键点，来应对尺度、旋转和光照变化；然后通过Fisher向量编码，把大量局部描述子聚合成为高维全局特征向量，去准确捕捉条纹的拓扑结构差异；最后把这个向量输入多层感知机进行非线性分类，完成个体身份判定。本模型基于ATRW（东北虎野外重识别）数据集展开实验，并与三种主流传统神经网络模型做了对比。